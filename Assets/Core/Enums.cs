public sealed class Enums
{

    #region Axis3

    public enum Axis3 { X, Y, Z };
    public enum Axis3Plane { XZ, XY, YZ };

    #endregion

    #region Axis2

    public enum Axis2 { X, Y };
    public enum Axis2Combine { XY, X, Y };

    #endregion
}
